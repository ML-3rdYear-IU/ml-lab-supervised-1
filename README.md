# ml-lab-supervised-1
Introduction to supervised machine learning using different supervised learning algorithms. Students will load a dataset, perform preprocessing, split data into training and testing sets, train a classification model, evaluate accuracy, and interpret results using Python and scikit-learn.
